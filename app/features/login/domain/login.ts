export class loginmodel{
    email!: String
    password!:String
}

export interface userInterface {
    contrasena: string;
    correo: string;
    id: string;
    lugar_residencia: string;
    nombre: string;
    numero_contacto: string;
}